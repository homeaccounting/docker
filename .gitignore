# Runtime configuration — never commit a filled-in .env
.env

# Assembled by scripts/caddy-assemble.sh from the active profiles' fragments
/conf.d/

# `just backup` writes here — a plaintext dump of the whole ledger
backup.sql.gz
